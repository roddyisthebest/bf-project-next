export enum PostType {
  Gallery = "gallery",
  Testimony = "testimony",
  Weekly = "weekly",
  Music = "music",
  Image = "image",
}

export enum ChapleType {
  Friday = "friday",
  Sunday = "sunday",
}

export enum UserRole {
  User = "user",
  Admin = "admin",
}
