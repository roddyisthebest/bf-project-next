export enum PostType {
  Talk = "talk",
  Gallery = "gallery",
  Testimonial = "testimonial",
}

export enum ChapleType {
  Friday = "friday",
  Sunday = "sunday",
}

export enum UserRole {
  User = "user",
  Admin = "admin",
}
